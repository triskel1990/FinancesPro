echo '[phases.setup]
nixPkgs = ["python311", "postgresql_15"]
[phases.install]
cmds = ["pip install -r requirements.txt"]
[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"' > nixpacks.toml

git add nixpacks.toml
git commit -m "Fix: ajouter postgresql_15 pour libpq.so"
git pull --rebase origin main
git push
